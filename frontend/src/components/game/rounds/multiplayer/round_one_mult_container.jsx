import { connect } from "react-redux";
//import Profile from './profile';
import RoundOne from "./round_one_mult";

const msp = (state, ownprops) => {
  return {};
};

const mdp = dispatch => {
  return {};
};

export default connect(msp, mdp)(RoundOne);
